const form = document.querySelector("form");

form.addEventListener("DOMEventListener", function (e) {
    e.preventDefault();

    const listItem = document.querySelectorAll(".list__item");
    const products = form.querySelector("#product");

    if (products.value === typeof Number) {

    }
})


